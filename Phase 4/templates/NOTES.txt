Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

Frontend Service: {{ include "todochatbot.fullname" . }}-frontend-service
Backend Service: {{ include "todochatbot.fullname" . }}-backend-service
Database Service: {{ include "todochatbot.fullname" . }}-database-service (if enabled)

To access the frontend application:
  minikube service {{ include "todochatbot.fullname" . }}-frontend-service --url

To access the backend API:
  minikube service {{ include "todochatbot.fullname" . }}-backend-service --url